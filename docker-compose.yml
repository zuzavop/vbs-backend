version: "3"
services:
  fastapi-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./server:/app  # Mount the local app directory to /app in the container
      - /mnt/datasets/vbs/:/data/vbs/
